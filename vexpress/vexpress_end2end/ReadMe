1. check the source_version file and get the sources from linux.org and uboot.org
2. compile the u-boot source with make ARCH=arm CROSS_COMPILE=arm-none-linux-gnueabi- vexpress_ca9x4_defconfig
3. compile the linux source with make ARCH=arm CROSS_COMPILE=arm-none-linux-gnueabi- vexpress_defconfig
4. compile the qemu using below commands: see the file qemusrc_compile.sh
5. enable bridge incase using nfs and tftp
	- enable_bridge.sh 
	- tftp_enable.sh 

6. copy the rootfs into nfs folder

7. copy the images into tftpboot folder

8. once enable bridge and compiled binaries use : cmd_for_fullrun.sh
	
